import { Box } from '@chakra-ui/react'
import styled from '@emotion/styled'

export const BioSection = styled(Box)`
  padding-left: 3.4em;
  text-indent: -3.4em;
`

export const BioYear = styled(Box)`
  font-weight: bold;
  margin-right: 1em;
`

export const OpeningTitle = styled.p`
  padding-left: 2em;
  margin-right: 1em;
  margin-top: 1em;
  margin-bottom: 1em;
`
